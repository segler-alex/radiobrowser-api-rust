## List of station check steps

> **Syntax:**

```json
<%= config[:api_server] %>/json/checksteps
```

```xml
<%= config[:api_server] %>/xml/checksteps
```

> **Results:**

Details at struct '[Check step](#station-check-step)'

```json
```

```xml
<result>
    <checkstep stepuuid=".." ... >
    <checkstep stepuuid=".." ... >
    ...
</result>
```

```shell
```

> **Example:**

```json
<%= config[:api_server] %>/json/checksteps
<%= config[:api_server] %>/json/checksteps?uuids=01234567-89ab-cdef-0123-456789abcdef
```

```xml
<%= config[:api_server] %>/xml/checksteps
<%= config[:api_server] %>/xml/checksteps?uuids=01234567-89ab-cdef-0123-456789abcdef
```

A list of steps that needed to be done to finish a station check.

Supported output formats: JSON, XML, CSV

### Parameters:

| Name | Example | Description |
|------|---------|-------------|
| uuids | `01234567-89ab-cdef-0123-456789abcdef,01234567-89ab-cdef-0123-456789abcdef` | MANDATORY, comma-separated list of UUIDs of stations, may be array in JSON |
